<% errors.forEach(function(x) { %>
    <div class="alert alert-danger text-center"><b><%= x %></b></div>
  <% }) %>

<% success.forEach(function(x) { %>
    <div class="alert alert-success text-center"><b><%= x %></b></div>
<% }) %>