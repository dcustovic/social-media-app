<%- include('partials/header') %>
  
  <div class="container py-md-5 container--narrow mt-4 text-center" id="blur">
	<%- include('partials/flash') %>
	<%- include('partials/profileShared') %>
  
    <div class="list-group">
			<% following.forEach(function(followedUser) {  %>
				<a href="/profile/<%= followedUser.username %>" class="list-group-item list-group-item-action">
					<div class="d-flex">
						<img class="avatar-tiny mr-3" src="<%= followedUser.avatar %>">
						<strong><%= followedUser.username %></strong>
					</div>
				</a>
			<% }) %>
		</div>
		
  </div>
  
  <%- include('partials/footer') %>